GROUNDING GATE: refused. {missing}

Run both + the listing, then ask again:

  {read} search --term '<literal>'{suffix}
  {read} search --query '<subject>'{suffix}
  {read} {listing}{suffix}

Empty search ≠ absence; a listing settles membership.

This turn carries: {have}
