NO GAP CAPTURE: refused. {draft} carries `kind: gap`: records a problem, leaves it standing.

next | by what the gap needs:

  repair somebody can make          make it
  question only its owner settles   put it to the owner
  decision that settles it          capture the decision, never the gap
