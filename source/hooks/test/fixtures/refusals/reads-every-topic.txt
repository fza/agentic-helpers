GROUNDING GATE: refused. No `--query` search ran this turn. No `--term` search ran this turn. No listing of every topic ran this turn.

Run both + the listing, then ask again:

  sdd search --term '<literal>'
  sdd search --query '<subject>'
  sdd view --layout "active:as-counts"

Empty search ≠ absence; a listing settles membership.

This turn carries: neither read
