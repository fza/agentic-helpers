---
type: signal
layer: conceptual
kind: fact
supersedes:
    - 20260925-144221-s-cpt-yz3
participants:
    - Felix
confidence: high
topics:
    - project/overview
preflight: dry-run-verified
summary: agentic-helpers is three machine-wide Claude Code hooks (carry-forward, scratch sweep, grounding gate) and the `agentic-capture` command, each with a skill and each opted into by its directory (`.memory/`, `.tmp/`, `.sdd/`); the gate and capture exist for sdd, and everything is Go, installed with `go install -C source/hooks ./cmd/...` and wired by `agentic-install`. It supersedes 20260925-144221-s-cpt-yz3, which described three Python hooks wired by `install.py`.
---

agentic-helpers is a set of machine-wide Claude Code hooks (carry-forward, scratch sweep, grounding gate) and the `agentic-capture` command, each paired with a skill that explains it to the agent. A project opts into each by carrying the directory it works on: `.memory/` for the carry-forward, `.tmp/` for the scratch sweep, `.sdd/` for the grounding gate and capture. A project without the directory is left alone, the binary exiting silently and creating nothing.

The grounding gate and `agentic-capture` exist for sdd, the Signal-Dialogue-Decision graph under `.sdd/`: the gate holds an agent to real `sdd` reads before it asks or drafts, and capture writes a draft through `sdd new`. A project names its topic prefix and draft lint in `.sdd/grounding.yaml`.

Everything is Go, built from `source/hooks` into `~/go/bin` with `go install -C source/hooks ./cmd/...`; `agentic-install`, run inside the checkout, wires the hooks into the global Claude Code settings and links the skills. The README carries install steps and the full behaviour of each piece.

It supersedes 20260925-144221-s-cpt-yz3, which described three plain-Python hooks wired by `install.py`: the hooks are Go binaries, `agentic-capture` joins them, and `agentic-install` replaces `install.py`.
