---
type: signal
layer: tactical
kind: done
closes:
    - 20260926-155456-d-tac-2au
participants:
    - Felix
confidence: high
topics:
    - hooks/grounding-gate
preflight: dry-run-verified
summary: This signal confirms the grounding gate now implements configurable `sdd show` depth and seat exemptions as specified, reading settings from `.sdd/grounding.yaml` and resolving seat claims from the main checkout. It closes the tactical directive (20260926-155456-d-tac-2au) by fulfilling its acceptance criteria, including depth validation, exemption logic, and seat detection, with all related tests passing and binaries deployed. The implementation maintains existing refusal rules for non-exempt operations while grounding seat checks in carry-forward claims.
---

Done for 20260926-155456-d-tac-2au: the grounding gate reads the `sdd show` depth it owes from a `show_depth` block in `.sdd/grounding.yaml`, and lets a session holding a seat named in `exempt_seats` show at any depth.

`graphconfig.Load` fills `show_depth.down` and `show_depth.up` from the file, keeping `2` and `1` for a key left out, an empty block or an empty file, and refuses a negative depth with `graphconfig.ErrNegativeDepth`. The gate reads the config only once a command carries a show, so a broken config never refuses a command reading nothing. A direction owing `0` is not checked, and the refusal in `refusals/downstream.txt` names the depth owed.

A new `internal/seat` package reads the claims under `.memory/roles/` for both `agentic-carryforward` and `agentic-grounding-gate`. The gate resolves the main checkout through `project.Root`, so a worktree session finds the claims its repository keeps, and matches the payload's session id, which a subagent shares. It runs that lookup only where `exempt_seats` names a seat. The exemption lifts the depth check alone: a discarded stream, `AskUserQuestion`, a draft `Write`, `Edit` or `NotebookEdit`, `agentic-capture` and a plain `sdd new` stay refused without the turn's reads.

Every acceptance criterion holds: `TestConfiguredShowDepth`, `TestANegativeDepthIsAConfigError`, `TestAnExemptSeat` and `TestSeatsLiveInTheMainCheckout` in `source/hooks/internal/grounding/showdepth_test.go`, `TestLoad` in `internal/graphconfig`, and `TestOfSession` and `TestHeld` in `internal/seat` cover them, against fixtures under `source/hooks/test/fixtures/grounding/`. Each new guard was broken, its test watched failing, and the file restored byte-identically; a guard no test could fail, an empty-seat check in the exemption, was removed. `go test -race ./...` passes in `source/hooks`, and `go install` has put the new binaries into `~/go/bin`. No hook wiring changed, so `agentic-install` has nothing to rewire. `README.md` and `skills/agent-grounding` describe the behaviour.

Commit: agentic-helpers 9d01b21.
