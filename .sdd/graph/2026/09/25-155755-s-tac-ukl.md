---
type: signal
layer: tactical
kind: done
closes:
    - 20260925-144721-d-tac-a9x
    - 20260925-153653-d-tac-piz
participants:
    - Felix
confidence: high
topics:
    - hooks/toolchain
preflight: dry-run-verified
summary: 'This signal confirms the complete migration of all hooks and their installer to Go binaries under `source/hooks`, eliminating all Python dependencies from the repository. It directly fulfills the Go-based implementation directive (20260925-144721-d-tac-a9x) and the `agentic-capture` standardization (20260925-153653-d-tac-piz), including enforcement of `listing_prefix: area-` and `draft_lint` checks while removing legacy capture scripts. The repository now relies exclusively on `go install -C source/hooks ./cmd/...` for installation.'
---

Every hook and its installer run as Go binaries from `source/hooks`, and no Python remains in the repository: `agentic-carryforward`, `agentic-grounding-gate`, `agentic-scratch`, `agentic-install`, and the `agentic-capture` command beside them. Shared code sits under `source/hooks/internal/`, fakes and fixtures under `source/hooks/test/`, and the Go rules in `source/AGENTS.md`, imported by `source/CLAUDE.md`. `hooks/`, `install.py` and `test.sh` are gone, `go test -race ./...` passes in `source/hooks`, and `agentic-install` wires the binaries from `~/go/bin` into `settings.json`.

The build command 20260925-144721-d-tac-a9x names, `go install ./source/hooks/cmd/...`, fails from the repository root, which carries no Go module; the working command is `go install -C source/hooks ./cmd/...`, and the README, AGENTS.md and the installer's own warning use it.

`agentic-capture` is in place as 20260925-153653-d-tac-piz describes: format-d-fdbox's `.sdd/grounding.yaml` carries `listing_prefix: area-`, which the capture enforces on every draft's topics, and `draft_lint: vale --output=line`; its `graph.py` and `graph-capture.py` are removed.

Commits: agentic-helpers 60b430e, 0139927, a39b059 and 9d51b29; format-d-fdbox 33be04d80.
