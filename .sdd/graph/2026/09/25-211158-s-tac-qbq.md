---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260925-210921-d-tac-pjn
      kind: addresses
      desc: carries it out; a settled directive is terminal
participants:
    - Felix
confidence: high
topics:
    - hooks/grounding-gate
preflight: dry-run-verified
summary: 'This signal confirms the `agentic-grounding-gate` now accepts `agentic-capture` of `kind: gap` drafts, treating them identically to other captures under the three-reads or grounded-draft rule. It directly carries out the tactical directive (20260925-210921-d-tac-pjn) by removing the gap-specific refusal logic, associated checks, and documentation, while preserving all other grounding-gate refusals. Evidence includes passing tests for all capture kinds, including gaps, and successful integration into the agentic-helpers codebase.'
---

Done for 20260925-210921-d-tac-pjn: `agentic-grounding-gate gate` no longer refuses an `agentic-capture` of a `kind: gap` draft, and holds it to the turn's three reads or a grounded draft like every other capture. The check and `refusals/gap.txt` are gone, the `agent-grounding` skill no longer carries the "Never capture a gap" rule or its row of what the gate refuses, and `README.md` no longer lists the refusal.

Evidence: `TestEveryKindCaptures` passes a capture of every kind, gap included; with the check restored it fails. Relative draft resolution, which the removed gap tests had exercised, is covered by grounded-draft tests for a capture from below the project root and for another checkout's draft; each fails when its path rule is broken. `go test -race ./...` passes in `source/hooks`, `go install` has put the new gate into `~/go/bin`, and agentic-helpers commit 29ddabd carries it.
